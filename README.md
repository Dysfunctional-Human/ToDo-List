# ToDo-List

To run:

For seeding: cargo run --bin seed
For app: cargo run

Write unit tests to make sure app doesn't crash when given wrong inputs.
Add a 'notes' features to be create a new notes section that opens up an editor ready to take notes
First deploy the project as a crate
Later, add authorisation and make it so the db is synced across different laptops 
Finally (most important) also turn it into a whatsapp messaging service to make it usable on mobile